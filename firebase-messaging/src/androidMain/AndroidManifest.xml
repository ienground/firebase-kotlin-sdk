<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <service
            android:name="zone.ien.firebase.messaging.FirebaseMessagingService"
            android:enabled="@bool/firebase_messaging_service_enabled"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
</manifest>
